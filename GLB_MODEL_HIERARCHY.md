# 📐 Иерархия GLB модели в Three.js

Данный документ описывает структуру и иерархию GLB (GL Transmission Format Binary) модели, как она загружается и обрабатывается в Three.js.

---

## 🏗️ Общая структура

GLB файл содержит всю информацию о 3D модели в бинарном формате:
- Геометрию (вершины, грани)
- Материалы и текстуры
- Анимации
- Иерархию объектов (ноды, меши, кости)

---

## 📊 Иерархия уровней

### 1. Верхний уровень: Scene (Сцена)

```javascript
Scene (THREE.Scene)
 └── Group (THREE.Group) - корневой узел модели
```

При загрузке через `useGLTF()`:
```javascript
const { scene, animations, materials: gltfMaterials } = useGLTF(modelPath);
```

Возвращает:
- **scene** — корневой объект (Group или Scene)
- **animations** — массив анимаций (AnimationClip[])
- **materials** — массив/объект всех материалов из GLB

---

### 2. Ноды (Nodes) — структура иерархии

Ноды представляют собой контейнеры для организации объектов в сцене:

```
Scene/Group
 ├── Node (Group/Object3D) - может содержать трансформации
 │   ├── Node (Group/Object3D)
 │   └── Mesh (THREE.Mesh) - визуальный объект
 ├── Bone (THREE.Bone) - для скелетной анимации
 └── Mesh (THREE.Mesh) - может быть на любом уровне
```

**Характеристики нодов:**
- Могут содержать трансформации (позиция, поворот, масштаб)
- Могут содержать дочерние ноды и меши
- Используются для организации сложных моделей
- Обход иерархии выполняется через `scene.traverse()`

---

### 3. Меши (Meshes) — визуальные объекты

Меш — это визуально отображаемый объект, который содержит геометрию и материал:

```
Mesh (THREE.Mesh)
 ├── geometry (THREE.BufferGeometry) - геометрия
 ├── material (THREE.Material | Material[]) - материал(ы)
 └── name (string) - имя меша
```

**Свойства меша:**
- `geometry` — определяет форму объекта
- `material` — один или массив материалов (для разных частей меша)
- `name` — идентификатор меша
- Наследует от `Object3D` (позиция, поворот, масштаб)

**Проверка меша в коде:**
```javascript
scene.traverse((child) => {
  if (child instanceof THREE.Mesh) {
    // Обработка меша
  }
});
```

---

### 4. Геометрия (Geometry) — структура формы

Геометрия определяет форму объекта через вершины и грани:

```
BufferGeometry
 ├── attributes
 │   ├── position (BufferAttribute) - ВЕРШИНЫ (x, y, z)
 │   ├── normal (BufferAttribute) - нормали для освещения
 │   ├── uv (BufferAttribute) - текстурные координаты (UV)
 │   ├── uv2 (BufferAttribute) - вторичные UV координаты
 │   └── color (BufferAttribute) - цвета вершин (опционально)
 ├── index (BufferAttribute) - индексы для граней
 └── groups[] - группы примитивов с разными материалами
     ├── { start: 0, count: 100, materialIndex: 0 }
     └── { start: 100, count: 50, materialIndex: 1 }
```

**Атрибуты геометрии:**
- **position** — массив координат вершин `[(x1,y1,z1), (x2,y2,z2), ...]`
- **normal** — нормали для каждой вершины (для расчета освещения)
- **uv** — координаты текстуры (0.0-1.0) для каждой вершины
- **index** — индексы вершин, формирующие грани (опционально)

**Groups (группы примитивов):**
- Позволяют использовать разные материалы для разных частей одного меша
- `start` — начальный индекс
- `count` — количество вершин в группе
- `materialIndex` — индекс материала из массива `mesh.material[]`

---

### 5. Вершины (Vertices) — точки в пространстве

Вершина — это точка в 3D пространстве с дополнительными данными:

**Обязательные данные:**
- **Position** (x, y, z) — координаты в 3D пространстве

**Опциональные данные:**
- **Normal** (nx, ny, nz) — нормаль для расчета освещения
- **UV** (u, v) — координаты для наложения текстуры (0.0-1.0)
- **Color** (r, g, b) — цвет вершины

**Доступ к вершинам:**
```javascript
const geometry = mesh.geometry;
const positions = geometry.attributes.position;
const vertexCount = positions.count; // количество вершин
```

---

### 6. Грани (Faces) — треугольники

Грань состоит из **3 вершин** и формирует поверхность:

**С индексами (indexed):**
- Используется `geometry.index` для определения граней
- Каждая грань = 3 индекса вершин
- Пример: `[0, 1, 2, 2, 3, 0]` → грани: (0,1,2) и (2,3,0)

**Без индексов (non-indexed):**
- Грани формируются последовательно из вершин
- Каждые 3 вершины = 1 грань

**Подсчет граней:**
```javascript
if (geometry.index) {
  faces = geometry.index.count / 3; // Каждая грань = 3 индекса
} else {
  faces = geometry.attributes.position.count / 3;
}
```

---

### 7. Материалы (Materials) — визуальные свойства

Материал определяет внешний вид поверхности:

```
Material (THREE.Material)
 ├── name (string) - имя материала
 ├── type (string) - тип материала
 │   ├── MeshStandardMaterial - PBR материал
 │   ├── MeshPhongMaterial - материал с бликами
 │   └── MeshBasicMaterial - простой материал
 └── textures[] - текстуры материала
     ├── map (diffuse/albedo) - основная текстура
     ├── normalMap - карта нормалей
     ├── roughnessMap - карта шероховатости
     ├── metalnessMap - карта металличности
     ├── aoMap - ambient occlusion карта
     └── emissiveMap - карта свечения
```

**Типы материалов:**
- **MeshStandardMaterial** — физически корректный рендеринг (PBR)
- **MeshPhongMaterial** — материал с бликами
- **MeshBasicMaterial** — простой материал без освещения

**Материал на меше:**
- Один материал: `mesh.material = material`
- Несколько материалов: `mesh.material = [material1, material2, ...]`
- При множественных материалах используется `geometry.groups[].materialIndex`

**Сбор материалов:**
```javascript
// Материалы на уровне меша
if (mesh.material) {
  const materials = Array.isArray(mesh.material) 
    ? mesh.material 
    : [mesh.material];
  materials.forEach((mat) => {
    if (mat && mat.name) {
      materialNames.add(mat.name);
    }
  });
}

// Материалы из geometry.groups (примитивы)
if (mesh.geometry && mesh.geometry.groups) {
  mesh.geometry.groups.forEach((group) => {
    if (group.materialIndex !== undefined && mesh.material) {
      const materials = Array.isArray(mesh.material) 
        ? mesh.material 
        : [mesh.material];
      const mat = materials[group.materialIndex];
      if (mat && mat.name) {
        materialNames.add(mat.name);
      }
    }
  });
}
```

---

### 8. Текстуры (Textures) — изображения

Текстура — это изображение, применяемое к материалу:

```
Texture (THREE.Texture)
 ├── image (Image/Canvas) - исходное изображение
 ├── wrapS, wrapT - режимы обертывания (Repeat, ClampToEdge)
 ├── minFilter, magFilter - фильтрация (Linear, Nearest)
 └── needsUpdate - флаг обновления
```

**Типы текстур:**
- **map** (diffuse/albedo) — основная текстура цвета
- **normalMap** — карта нормалей для деталей поверхности
- **roughnessMap** — карта шероховатости (PBR)
- **metalnessMap** — карта металличности (PBR)
- **aoMap** — ambient occlusion для теней
- **emissiveMap** — карта свечения
- **bumpMap** — карта рельефа (устаревшая, используйте normalMap)
- **displacementMap** — карта смещения вершин
- **alphaMap** — карта прозрачности
- **lightMap** — карта освещения
- **envMap** — карта окружения (отражения)

**Извлечение текстур:**
```javascript
const textureProperties = [
  'map', 'normalMap', 'roughnessMap', 'metalnessMap', 
  'aoMap', 'emissiveMap', 'bumpMap', 'displacementMap',
  'alphaMap', 'envMap', 'lightMap', 'specularMap'
];

textureProperties.forEach(prop => {
  const texture = material[prop];
  if (texture instanceof THREE.Texture) {
    textures.add(texture);
  }
});
```

---

## 🔗 Полная иерархия GLB модели

```
GLB File
│
├── Scene (THREE.Scene)
│   └── Root Group (THREE.Group)
│       │
│       ├── Node/Group (THREE.Object3D/Group)
│       │   │   (трансформации: позиция, поворот, масштаб)
│       │   │
│       │   ├── Mesh (THREE.Mesh) ⭐
│       │   │   ├── geometry (BufferGeometry)
│       │   │   │   ├── attributes
│       │   │   │   │   ├── position → [вершины: (x,y,z)...]
│       │   │   │   │   ├── normal → [нормали: (nx,ny,nz)...]
│       │   │   │   │   ├── uv → [UV координаты: (u,v)...]
│       │   │   │   │   └── color → [цвета вершин...]
│       │   │   │   ├── index → [индексы граней: 0,1,2, 2,3,0...]
│       │   │   │   └── groups[] → [{start, count, materialIndex}]
│       │   │   │
│       │   │   └── material (Material | Material[])
│       │   │       ├── name: "Material_1"
│       │   │       ├── type: "MeshStandardMaterial"
│       │   │       └── textures
│       │   │           ├── map → Texture (diffuse)
│       │   │           ├── normalMap → Texture
│       │   │           ├── roughnessMap → Texture
│       │   │           └── metalnessMap → Texture
│       │   │
│       │   └── name: "Mesh_01"
│       │
│       ├── Bone (THREE.Bone) - для скелетной анимации
│       │
│       └── Mesh (THREE.Mesh) - может быть напрямую в Group
│
├── Materials[] (массив всех материалов из GLB)
│   ├── Material_1
│   ├── Material_2
│   └── ...
│
├── Animations[] (массив анимаций)
│   └── AnimationClip
│
└── Textures[] (встроенные в GLB или внешние)
    ├── texture_01.png
    └── texture_02.png
```

---

## 🔄 Связи между компонентами

### 1. Меш → Геометрия → Вершины/Грани
```javascript
const mesh = ...;
const geometry = mesh.geometry;
const vertices = geometry.attributes.position; // массив вершин
const faces = geometry.index; // индексы граней
```

### 2. Меш → Материал(ы)
```javascript
const material = mesh.material; // один материал
const materials = mesh.material; // или массив материалов
```

### 3. Геометрия → Группы → Материалы
```javascript
geometry.groups.forEach((group) => {
  const materialIndex = group.materialIndex;
  const material = materials[materialIndex];
});
```

### 4. Материал → Текстуры
```javascript
const diffuseTexture = material.map;
const normalTexture = material.normalMap;
const roughnessTexture = material.roughnessMap;
```

### 5. Ноды → Меши
```javascript
scene.traverse((child) => {
  if (child instanceof THREE.Mesh) {
    // Обработка меша
  }
});
```

---

## 📊 Пример обработки в коде

### Сбор статистики модели:
```javascript
scene.traverse((object) => {
  if (object instanceof THREE.Mesh) {
    meshes++;
    
    // Вершины
    if (geometry.attributes.position) {
      vertices += geometry.attributes.position.count;
    }
    
    // Грани
    if (geometry.index) {
      faces += geometry.index.count / 3;
    }
    
    // Материалы
    if (Array.isArray(object.material)) {
      object.material.forEach(mat => materials.add(mat));
    } else if (object.material) {
      materials.add(object.material);
    }
  }
});
```

### Сбор материалов меша:
```javascript
function collectMeshMaterials(mesh) {
  const materialNames = new Set();
  
  // 1. Материалы на уровне меша
  if (mesh.material) {
    const materials = Array.isArray(mesh.material) 
      ? mesh.material 
      : [mesh.material];
    materials.forEach((mat) => {
      if (mat && mat.name) {
        materialNames.add(mat.name);
      }
    });
  }
  
  // 2. Материалы из geometry.groups (примитивы)
  if (mesh.geometry && mesh.geometry.groups) {
    mesh.geometry.groups.forEach((group) => {
      if (group.materialIndex !== undefined && mesh.material) {
        const materials = Array.isArray(mesh.material) 
          ? mesh.material 
          : [mesh.material];
        const mat = materials[group.materialIndex];
        if (mat && mat.name) {
          materialNames.add(mat.name);
        }
      }
    });
  }
  
  return materialNames;
}
```

---

## 🎯 Ключевые концепции

### Object3D vs Mesh
- **Object3D** — базовый класс для всех 3D объектов (позиция, поворот, масштаб)
- **Mesh** — специализированный Object3D с геометрией и материалом
- **Все меши — это объекты, но не все объекты — это меши**

### Single vs Multiple материалы
- **Single материал** — один материал на весь меш
- **Multiple материалы** — массив материалов, разные части меша используют разные материалы через `geometry.groups`

### Groups (группы примитивов)
- Позволяют использовать разные материалы для разных частей одного меша
- Каждая группа определяет диапазон вершин и индекс материала
- Используется для сложных моделей с несколькими текстурами

---

## 📚 Дополнительные ресурсы

- [Three.js Documentation](https://threejs.org/docs/)
- [GLTF Specification](https://www.khronos.org/gltf/)
- [Three.js BufferGeometry](https://threejs.org/docs/#api/en/core/BufferGeometry)
- [Three.js Materials](https://threejs.org/docs/#api/en/materials/Material)

---

**Версия документа:** 1.0  
**Дата создания:** 2024  
**Автор:** React Animation Project

