# 📚 Инструкция по использованию Material Manager для Blender

## 🎯 Описание
Скрипт добавляет панель управления материалами в Blender с тремя функциями:
- **Multiple** - применяет множественные материалы из папки для переключения в вьювере
- **Single** - применяет один постоянный материал (не переключается)
- **Export GLB** - экспортирует выделенные объекты в GLB формат

---

## 📋 Требования

1. **Blender версии 3.0+**
2. **Папки с текстурами должны существовать:**
   - `C:\Users\HP\Downloads\дивансон\textures\multiple` - для множественных материалов
   - `C:\Users\HP\Downloads\дивансон\textures\single` - для одного материала

---

## 🚀 Установка и запуск

### Шаг 1: Подготовка папок с текстурами

Создайте структуру папок:
```
C:\Users\HP\Downloads\дивансон\textures\
  ├── multiple\     (все текстуры для переключения)
  │   ├── кожа_01.png
  │   ├── ткань01.png
  │   └── ...
  └── single\       (одна текстура для постоянного материала)
      └── base_texture.png
```

**Важно:**
- Имена файлов в папке `multiple` станут именами материалов (без расширения)
- В папке `single` должен быть один файл (если несколько - используется первый)
- Поддерживаемые форматы: `.png`, `.jpg`, `.jpeg`, `.bmp`, `.tiff`, `.tif`

### Шаг 2: Запуск скрипта

1. **Откройте Blender**
2. **Перейдите на вкладку "Scripting"** (в верхнем меню)
3. **Откройте скрипт:**
   - `Text` → `Open...`
   - Выберите файл `blender_material_manager.py`
4. **Запустите скрипт:**
   - Нажмите кнопку **"Run Script"** (▶) или используйте `Alt + P`
5. **Откройте панель:**
   - Нажмите клавишу **`N`** (откроет боковую панель Properties)
   - Внизу найдите вкладку **"Material Manager"**

---

## 🎨 Использование

### 1. Применение множественных материалов (Multiple)

**Когда использовать:**
- Когда нужно иметь возможность переключать материалы в вьювере
- Для объектов, где несколько вариантов текстур

**Процесс:**
1. Выделите объект(ы) в Blender (в режиме Object Mode)
2. Переключитесь на панель "Material Manager" (клавиша `N`)
3. Нажмите кнопку **"Multiple"**
4. Скрипт:
   - Найдет все текстуры в папке `multiple`
   - Создаст материалы с именами из файлов текстур
   - Применит все материалы к выделенным объектам
5. После экспорта в GLB эти материалы будут доступны для переключения в вьювере

**Пример:**
- Папка `multiple` содержит: `кожа_01.png`, `ткань01.png`
- После нажатия "Multiple" создаются материалы: `кожа_01`, `ткань01`
- В вьювере будет dropdown с опциями: `кожа_01`, `ткань01`

### 2. Применение одного материала (Single)

**Когда использовать:**
- Когда нужен один постоянный материал, который нельзя переключить
- Для объектов, где текстура должна быть фиксированной

**Процесс:**
1. Выделите объект(ы) в Blender
2. Переключитесь на панель "Material Manager"
3. Нажмите кнопку **"Single"**
4. Скрипт:
   - Найдет первый файл текстуры в папке `single`
   - Создаст материал с именем из файла (с префиксом `__SINGLE__`)
   - Применит этот материал ко всем выделенным объектам
5. После экспорта этот материал НЕ будет виден в меню переключения

**Пример:**
- Папка `single` содержит: `base_texture.png`
- После нажатия "Single" создается материал: `__SINGLE__base_texture`
- В вьювере этот материал НЕ будет в списке для переключения
- Объект всегда будет отображаться с этой текстурой

### 3. Экспорт в GLB

**Процесс:**
1. Выделите объект(ы) для экспорта
2. Нажмите кнопку **"Export GLB"**
3. Откроется диалог выбора файла
4. Выберите путь и имя файла (по умолчанию: `C:\Users\HP\Downloads\дивансон\export.glb`)
5. Нажмите "Export glTF 2.0"
6. GLB файл будет создан со всеми материалами

**Что экспортируется:**
- Геометрия выделенных объектов
- Все материалы (multiple и single)
- Текстуры встроены в GLB файл
- Single материалы имеют префикс `__SINGLE__` и не показываются в меню

---

## 📝 Особенности работы

### Имена материалов

- **Multiple материалы:** Имя = имя файла текстуры (без расширения)
  - `кожа_01.png` → материал `кожа_01`
  - `ткань01.png` → материал `ткань01`

- **Single материалы:** Имя = `__SINGLE__` + имя файла
  - `base_texture.png` → материал `__SINGLE__base_texture`
  - Этот префикс скрывает материал от отображения в вьювере

### Распределение материалов на гранях

- **Multiple:** Все материалы добавляются к объекту. По умолчанию первый материал применяется ко всем граням
  - Для ручного распределения перейдите в Edit Mode и назначьте материалы через Material Properties

- **Single:** Один материал применяется ко всем граням объекта

### Поведение в вьювере

После загрузки GLB в приложение:

1. **Объекты с Multiple материалами:**
   - В dropdown будут видны все материалы (кроме single)
   - Можно переключаться между материалами
   - При снятии выбора восстанавливаются оригинальные материалы

2. **Объекты с Single материалами:**
   - НЕ видны в dropdown (префикс `__SINGLE__`)
   - Материал всегда применяется и не может быть изменен
   - Не участвуют в переключении материалов

---

## ⚙️ Настройка путей

Если нужно изменить пути к папкам, отредактируйте переменные в начале скрипта:

```python
MULTIPLE_TEXTURES_DIR = r"C:\Users\HP\Downloads\дивансон\textures\multiple"
SINGLE_TEXTURE_DIR = r"C:\Users\HP\Downloads\дивансон\textures\single"
EXPORT_DIR = r"C:\Users\HP\Downloads\дивансон"
```

---

## 💡 Примеры использования

### Пример 1: Диван с переключаемыми текстурами

1. Поместите текстуры в `multiple`: `кожа.png`, `ткань.png`, `велюр.png`
2. Выделите диван в Blender
3. Нажмите "Multiple"
4. Экспортируйте через "Export GLB"
5. В вьювере будет dropdown с опциями: `кожа`, `ткань`, `велюр`

### Пример 2: Основание дивана с фиксированной текстурой

1. Поместите текстуру в `single`: `дерево.png`
2. Выделите основание дивана
3. Нажмите "Single"
4. Экспортируйте через "Export GLB"
5. В вьювере основание всегда будет с текстурой `дерево`, без возможности переключения

### Пример 3: Комплексная модель

1. Диван (Multiple): `кожа.png`, `ткань.png`
2. Основание (Single): `дерево.png`
3. Подушки (Multiple): `белый.png`, `серый.png`
4. Экспорт всех объектов вместе
5. В вьювере:
   - Диван: dropdown с `кожа`, `ткань`
   - Основание: фиксированная текстура `дерево`
   - Подушки: dropdown с `белый`, `серый`

---

## ⚠️ Частые проблемы

### Проблема: "Нет выделенных объектов типа MESH"
**Решение:** Убедитесь, что выделены объекты типа Mesh. Убедитесь, что вы в Object Mode (не Edit Mode)

### Проблема: "Папка не найдена"
**Решение:** Проверьте, что папки существуют по указанным путям

### Проблема: "Текстуры не найдены"
**Решение:** 
- Проверьте форматы файлов (png, jpg, jpeg, bmp, tiff, tif)
- Убедитесь, что файлы находятся в правильных папках

### Проблема: Материалы не переключаются в вьювере
**Решение:** 
- Убедитесь, что использовали кнопку "Multiple", а не "Single"
- Проверьте, что материалы не имеют префикс `__SINGLE__`
- Убедитесь, что в папке `multiple` есть текстуры

### Проблема: Single материалы видны в меню
**Решение:** Проверьте, что код в `ModelViewer.tsx` фильтрует материалы с префиксом `__SINGLE__`

---

## 🔄 Обновление скрипта

После изменения скрипта:
1. Закройте Blender (если открыт)
2. Откройте скрипт заново
3. Запустите `Run Script`
4. При необходимости перезагрузите панель: `F3` → `Reload Scripts`

---

## 📚 Дополнительно

### Поддерживаемые форматы изображений:
- PNG (.png)
- JPEG (.jpg, .jpeg)
- BMP (.bmp)
- TIFF (.tiff, .tif)

### Совместимость:
- Blender 3.0+
- GLB формат (совместим с Three.js, React Three Fiber)
- Все материалы экспортируются с встроенными текстурами

---

## 🎯 Итоговая схема работы

```
Blender:
1. Выделить объект → Multiple/Single → Экспорт GLB
                                    ↓
                          GLB файл с материалами
                                    ↓
                    React App (ModelViewer):
                          ↓                ↓
              Multiple материалы    Single материалы
              (в dropdown)         (скрыты, фиксированы)
```

---

## 💻 Технические детали

- **Префикс single материалов:** `__SINGLE__` - используется для идентификации и скрытия от меню
- **Структура материалов:** Все материалы создаются с использованием Principled BSDF (PBR)
- **Экспорт:** Используется стандартный экспортер Blender GLTF 2.0

